spring:
  datasource:
    url: jdbc:mysql://localhost:8889/petregister
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true